# Where the API lives.
#
# Leave this unset in development: the app reads the machine's address off the
# Metro packager URL, so a phone on the same Wi-Fi reaches your computer rather
# than itself. Set it when running against a deployed API, or on a simulator
# that cannot see the packager host.
# EXPO_PUBLIC_SERVER_URL=https://api.avermate.fr
