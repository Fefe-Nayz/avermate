# Secrets — every app keeps its own, none of them are committed
.env
.env.local
.env.*.local

# Dependencies and build output, at any depth in the workspace
node_modules/
.turbo/
dist/
.next/

# Local databases
*.db
*.db-journal
*.db-shm
*.db-wal

# Editor
.vscode/settings.json
.vscode/tasks.json
.hintrc

# Agent scratch
/.agents
